# Supabase — auth
NEXT_PUBLIC_SUPABASE_URL=https://bunibppyamcpbajjcptl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# AWS Athena — live order + sales data (same credentials as Sheet Society Fold)
AWS_ATHENA_REGION=ap-southeast-2
AWS_ATHENA_ACCESS_KEY_ID=your-access-key-id
AWS_ATHENA_SECRET_KEY=your-secret-access-key

# Postgres — Replit built-in DB (same as Sheet Society Fold — shares tables)
DATABASE_URL=postgresql://user:pass@host:5432/dbname
